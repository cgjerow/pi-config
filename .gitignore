# Machine-specific config
models-store.json
models_override.json

# Secrets (override with env vars where possible)
auth.json

# Session history
sessions/

# Node modules (if any package has deps)
node_modules/
