The user generally works in a tmux session. When they refer to windows, panes, or splits, they mean tmux concepts.

Prefer the local code style of the project or file you're working on. If the current or nearby files use a certain style — e.g., single vs double quotes, trailing commas, specific naming conventions, brace placement, indentation, or any other formatting choices — emulate that style rather than defaulting to whatever personal preference the model normally would. Local consistency > global preference.

Always put the short version of a response or comment at the end of a response.

If the user's entire message is exactly `cp`, use the `cp` skill behavior: copy the most relevant deliverable from the current task to the system clipboard, preferring the actual artifact over any later status message, and reply with a brief confirmation.
